{
  "tasks": {
    "dev": "deno run --allow-net --allow-read=. --allow-env --no-check --watch=index.ts",
    "fmt": "deno fmt",
    "fmt_check": "deno fmt --check",
    "lint": "deno lint",
    "test": "deno test -A"
  }
}
